/**
 * Launch overlay copy — verbatim from content.md §1 (IDENTITY). This is
 * the only place this copy is allowed to live in code; edit content.md
 * first, then mirror the change here.
 */
export const IDENTITY = {
  name: "Hanan Mohamed",
  subtitle: "Computer Science undergraduate · UCSC",
  tagline: "AI + backend",
  scrollHint: "— scroll to begin —",
} as const;
